<% unless ext.callbacks.empty? %>
@callbacks = {
  <% max_len = ext.max_callback_name_length %>
  <% ext.callbacks.each do |_, cb| %>
  <%= cb.mapped(max_len) %>,
  <% end %>
}.freeze
<% end %>