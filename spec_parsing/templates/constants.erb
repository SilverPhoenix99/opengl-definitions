<% unless ext.constants.empty? %>
  <% max_len = ext.max_constant_name_length %>
  <% ext.constants.each do |name, value| %>
<%= name + ' ' * (max_len - name.length) %> = <%= value %>
  <% end %>
<% end %>