<%= render :constants, locals: { req: req } unless req.constants.empty? %>

<%= render :callbacks, locals: { req: req } unless req.callbacks.empty? %>

<%= render :functions, locals: { req: req } unless req.functions.empty? %>